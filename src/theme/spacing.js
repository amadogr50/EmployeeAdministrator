import dimensions from './dimensions';

const spacing = {
  xxs: `${dimensions.xxs}px`,
  xs: `${dimensions.xs}px`,
  s: `${dimensions.s}px`,
  m: `${dimensions.m}px`,
  l: `${dimensions.l}px`,
  xl: `${dimensions.xl}px`,
  xxl: `${dimensions.xxl}px`,
};

export default spacing;
