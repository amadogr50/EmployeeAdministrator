import React from 'react';
import styled from 'styled-components/native';

const Title = styled.Text`
  font-family: Arial;
  font-weight: 700;
  font-size: 18px;
`;

const Body = styled.Text`
  font-family: Arial;
  font-weight: 400;
  font-size: 12px;
`;

const Input = styled.Text`
  font-family: Arial;
  font-weight: 700;
  font-size: 14px;
`;

export {Title, Body, Input};
